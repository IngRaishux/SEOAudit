{
  "name": "seoaudit",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "cd apps/web && npm run build",
    "dev": "cd apps/web && npm run dev",
    "start": "node_modules/.bin/next start --dir apps/web"
  },
  "devDependencies": {
    "next": "16.2.5"
  }
}
